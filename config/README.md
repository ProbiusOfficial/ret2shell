## config.sample.toml

服务器配置文件样例

## priv.bin

license签发私钥，本仓库仅供测试使用，正式环境请使用 `r2s-keygen` 重新生成；

## pub.bin

license验证公钥，本仓库仅供测试使用，正式环境请使用 `r2s-keygen` 重新生成并重新编译分发二进制；

## sensitive_word_list.txt

敏感词检测词库
